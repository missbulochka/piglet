version: '3.8'

services:
  psql:
    image: "postgres:16.2"
    env_file:
      - psql.env
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  piglet-auth:
    build:
      context: ../
      dockerfile: ./docker/piglet-auth.Dockerfile
    env_file:
      - auth.env
    depends_on:
      psql:
        condition: service_healthy
    restart: unless-stopped
